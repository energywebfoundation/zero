import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  popoverText: {
    // should consume from theme
    fontSize: '12px',
    fontFamily: 'Rajdhani',
    fontWeight: 600,
    textAlign: 'center',
  },
});
