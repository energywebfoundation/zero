import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
  root: {
    // should be change to consume theme, or in config level
    backgroundColor: '#F6F3F9',
  },
});
