import styled from '@emotion/styled';

/* eslint-disable-next-line */
export interface UiAccountProps {}

const StyledUiAccount = styled.div``;

export const UiAccount = (props: UiAccountProps) => (
  <StyledUiAccount></StyledUiAccount>
);

export default UiAccount;
